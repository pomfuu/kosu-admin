specVersion: "v1beta"
serviceId: "kosu-admin"
location: "asia-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "kosu-admin-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
